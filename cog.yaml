build:
  gpu: true
  python_version: "3.12"
  python_packages:
    - "torch==2.6.0"
    - "safetensors"
    - "tiktoken"
    - "numpy"
    - "huggingface_hub"
    - "packaging"
    - "tqdm"
  run:
    - "pip install git+https://github.com/openai/privacy-filter.git"
predict: "predict.py:Predictor"
image: "r8.im/zeke/openai-privacy-filter"
